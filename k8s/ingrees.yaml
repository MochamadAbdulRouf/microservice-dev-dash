apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devdash-ingrees
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - host: devdash.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api-service
            port:
              number: 3000